if (event.sensor.getType()==Sensor.TYPE_ROTATION_VECTOR) {
  rotEvent((double)event.values[0], (double)event.values[1], (double)event.values[2],
   (double)event.values[3], (double)event.values[4]);
   
   //nativeEvent(40,41,0); // EVENT_SENSOR, SEN_ROTATION  //send a heartbeat
   
}
